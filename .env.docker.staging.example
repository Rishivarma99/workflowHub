# Copy to .env.docker.staging for docker-compose.staging.yml
#   Copy-Item .env.docker.staging.example .env.docker.staging
#
# nginx proxies /api/* to this URL (no trailing slash).

API_PROXY_TARGET=https://your-workflowhub-staging-api.onrender.com
